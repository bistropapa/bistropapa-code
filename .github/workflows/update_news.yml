name: Update News Daily

on:
  schedule:
    - cron: '0 22 * * *'
  workflow_dispatch:

jobs:
  update-news:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Fetch news and generate news.json
        env:
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
        run: |
          python3 - <<'EOF'
          import urllib.request
          import json
          import os
          from datetime import datetime, timezone, timedelta

          JST = timezone(timedelta(hours=9))
          now = datetime.now(JST).strftime('%Y年%m月%d日 %H:%M')
          api_key = os.environ.get('NEWS_API_KEY', '')
          url = f"https://newsapi.org/v2/top-headlines?country=jp&pageSize=10&apiKey={api_key}"

          try:
              with urllib.request.urlopen(url, timeout=10) as res:
                  data = json.loads(res.read())
              articles = []
              for a in data.get('articles', []):
                  if not a.get('title') or a['title'] =
